# 智学方舟前端

## 启动

```powershell
cd frontend
npm.cmd install
npm.cmd run dev
```

后端地址默认是 `http://localhost:8000`。需要修改时，复制 `.env.example` 为 `.env.local` 并调整 `VITE_API_BASE_URL`。

## 构建与测试

```powershell
npm.cmd run test
npm.cmd run build
```
